# `@overkill-dev/reporter-tree`

Final-result logical tree reporter for Overkill test runs.

```ts
import { createTreeReporter } from '@overkill-dev/reporter-tree';
```

The reporter declares `stdout-raw`. It prints the logical result tree, a
detailed problem section, and final counts with timing.
