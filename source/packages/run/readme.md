# `@overkill-dev/run`

Run orchestration package for turning caller intent into resolved Overkill runs.

Top-level API:

- `RunRequest`
- `RunFacts`
- `ResolvedRun`
- `resolveRun(request)`
- `run(request)`
